import 'package:flutter/material.dart';

class GroupMain extends StatelessWidget {
  const GroupMain({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text('group main');
  }
}
